# Byteman

A simple and secure command-line password manager. 
Current commands are:

byteman [--verbose] [--version] [--help] <command> [<args>]
byteman signup
byteman login

OpenSSL is used as a dependency to help secure passwords and more
hence must be installed.